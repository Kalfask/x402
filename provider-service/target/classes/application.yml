server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/x402_auth?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
app:
  jwt.secret: ${JWT_SECRET}