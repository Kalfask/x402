global:
  scrape_interval: 5s # Check every 5 seconds for faster testing

scrape_configs:
  - job_name: 'spring-boot-gateway'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']